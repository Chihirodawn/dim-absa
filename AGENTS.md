# AI 项目协作规则

本文件是本项目中所有 AI 编程工具的共同工作规则。
无论当前使用 Codex、ZCode、WorkBuddy 或其他 Agent，都应优先遵守本文件。

## 一、信息可信度优先级

当不同信息发生冲突时，按以下顺序判断：

1. 当前代码、配置文件和实际运行结果
2. 当前 Git 状态、提交记录和差异
3. `.ai/HANDOFF.md` 中最近一次交接记录
4. `.ai/PROJECT_STATE.md` 中的项目状态
5. `.ai/TASKS.md` 中的任务记录
6. `.ai/DECISIONS.md` 中的历史决策
7. 当前聊天中的描述
8. AI 自己的推测

不得因为交接记录声称“已经完成”，就跳过对代码和测试结果的检查。

## 二、开始任务前必须执行

在修改代码之前：

1. 阅读本文件 `AGENTS.md`。
2. 阅读 `.ai/PROJECT_STATE.md`。
3. 阅读 `.ai/TASKS.md`。
4. 阅读 `.ai/HANDOFF.md`。
5. 在涉及技术方案或历史取舍时，阅读 `.ai/DECISIONS.md`。
6. 检查当前工作区的 Git 状态和最近提交记录。
7. 确认当前任务、已完成内容、未验证内容和下一步。

如上述文件尚未填写，应根据当前项目实际情况补充，但不要凭空编造信息。

## 三、工作原则

- 只修改与当前任务有关的内容，避免顺手进行无关重构。
- 优先沿用项目现有架构、依赖、代码风格和命名方式。
- 修改前先定位调用链和影响范围。
- 不得把推测写成已验证结论。
- 不得为了让测试表面通过而删除有效测试或绕过核心逻辑。
- 不得擅自删除用户文件、数据、模型、数据库或重要配置。
- 不得在没有说明影响的情况下升级核心依赖。
- 不得读取、输出或提交密码、Token、私钥、Cookie、`.env` 等敏感信息。
- 发现当前任务描述与实际代码不一致时，应以代码和运行结果为依据，并记录差异。
- 大范围修改前，应先给出简短计划；小范围、明确修改可直接执行。

## 四、Git 安全规则

- 可以读取 `git status`、`git diff`、`git log` 和 `git show`。
- 未经用户明确要求，不执行 `git push`、强制推送或创建远程仓库。
- 未经用户明确许可，不执行 `git reset --hard`、`git clean -fd`、覆盖式 checkout 等可能丢失工作的命令。
- 未经用户明确要求，不擅自合并分支或改写历史。
- 是否执行 `git commit` 以用户当前要求为准；即使不提交，也要保留清晰的修改说明。
- 如果工作区在开始任务前已有未提交修改，不得把它们误认为本次 AI 的修改。

## 五、验证要求

完成修改后，应尽可能执行与本次任务相关的检查，例如：

- 单元测试或集成测试
- 编译或构建
- 类型检查
- 代码格式检查
- 最小运行示例
- 与本次问题直接相关的命令

对于不能执行的验证，必须说明：

- 哪项没有验证
- 为什么没有验证
- 用户之后可以怎样验证

“代码看起来没问题”不能代替实际验证。

## 六、结束任务前必须执行

每次形成阶段性成果后：

1. 更新 `.ai/TASKS.md` 中对应任务的状态。
2. 更新 `.ai/HANDOFF.md`，让下一个 AI 能直接接手。
3. 当项目结构、运行方法或整体状态改变时，更新 `.ai/PROJECT_STATE.md`。
4. 当出现重要技术取舍时，更新 `.ai/DECISIONS.md`。
5. 汇报本次修改文件、验证命令、验证结果、未解决问题和建议下一步。

即使本次任务没有完成，也必须留下交接记录，写明卡在哪里、尝试过什么以及下一步建议。

## 七、交接记录要求

`.ai/HANDOFF.md` 至少应包含：

- 当前目标
- 本次已经完成的工作
- 修改过的文件
- 执行过的命令和测试
- 已通过、失败和未执行的检查
- 当前已知问题
- 尚未验证的假设
- 已经失败或不建议重复的方案
- 推荐的下一步
- 当前 Git 分支、最近提交和工作区状态

交接记录应简洁、具体，不要复制整段聊天，也不要保存无关思考过程。

## 八、项目记忆维护原则

- `AGENTS.md`：只记录长期稳定的协作规则。
- `.ai/PROJECT_STATE.md`：记录项目当前整体状态。
- `.ai/TASKS.md`：记录任务及其验收标准。
- `.ai/HANDOFF.md`：记录最近一次工作的具体交接。
- `.ai/DECISIONS.md`：记录重要技术决策及其原因。

动态信息不要全部堆进 `AGENTS.md`，避免文件过长和规则失效。

## 九、本项目专属约束

- 上游 `resources/DimABSA2026/` 视为只读官方资源，不直接修改，不重新发布原始数据。
- 当前实验固定为 Track A / Task 1 / 中文餐厅；扩展任务前先记录新范围。
- 公开 test 金标准不得用于 Prompt、few-shot 示例、阈值或校准参数选择。
- Qwen 模型推理默认 `smoke`；完整付费 GPU 推理必须由用户明确确认，并要求 `CONFIRM_FULL_RUN=YES`。
- diagnostics 中只要存在 `fallback_train_mean`，该次结果就不能直接当成正式模型结果。
